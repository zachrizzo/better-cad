{
  "name": "better-cad",
  "private": true,
  "version": "0.1.0",
  "description": "Free, cross-platform CAD tool for architects — native Rust + wgpu + egui",
  "scripts": {
    "proxy": "cd proxy && npm start",
    "build": "cargo build -p bcad-app --release",
    "dev": "node proxy/bettercad-ai-proxy.mjs & cargo run -p bcad-app",
    "test": "cargo test --workspace"
  }
}
