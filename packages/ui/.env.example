# Recommended: local AI proxy using Claude Code OAuth
VITE_AI_PROXY_WS=ws://localhost:3001

# Optional: direct Anthropic API from the browser instead of the local proxy
# VITE_ANTHROPIC_API_KEY=your_anthropic_api_key_here
